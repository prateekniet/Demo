apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-all-ingress
  namespace: netpol-demo   # replace with your namespace
spec:
  podSelector: {}          # select all pods in the namespace
  policyTypes:
  - Ingress
  ingress: []              # empty list = deny all ingress

